import PackageDemo from "../_components/PackageDemo";

export const metadata = { title: "Package page 2 — One topic at a time" };

export default function Page() {
  return <PackageDemo variant={2} />;
}
